/*

OPERADOR TERNÁRIO

*/

// Ternário IF ELSE
const chuva = false;

// console.log(chuva ? "Vai chover" : "Dia ensolarado");
// console.log(condição ? se true executa aqui : senão executa aqui);

// _____________________________________________

// Ternário somente com o IF

chuva && console.log("Vai chover");
// Se for false, não irá aparecer nada.
