/*

Diferenças entre var, let e const:
- var: é uma variável que pode ser redeclarada e reatribuída.
- let: é uma variável que não pode ser redeclarada, mas pode ser reatribuída
- const: é uma constante que não pode ser redeclarada e nem reatribuída

Mais características ou exemplos:
- var: tem um escopo de função, ou seja, se você declarar uma variável dentro de uma função, ela só será acessível dentro daquela função.
- let e const: tem um escopo de bloco, ou seja, se você declarar uma variável dentro de um bloco (if, for, while, etc), ela só será acessível dentro daquele bloco.

Porque não se usa var:
- var pode causar problemas devido ao seu escopo de função, pois pode ser acessada em qualquer lugar da função, mesmo que tenha sido declarada em um bloco.
- var pode ser redeclarada, o que pode causar problemas de legibilidade e manutenção do código.

*/
